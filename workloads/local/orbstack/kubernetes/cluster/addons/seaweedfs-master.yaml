apiVersion: apps/v1
kind: Deployment
metadata:
  name: seaweedfs-master
  namespace: seaweedfs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: seaweedfs-master
  template:
    metadata:
      labels:
        app: seaweedfs-master
    spec:
      containers:
        - name: seaweedfs-master
          image: docker.io/chrislusf/seaweedfs:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9333
              name: http
            - containerPort: 19333
              name: grpc
          command:
            - weed
            - master
            - -mdir=/data/master
            - -defaultReplication=000
          volumeMounts:
            - mountPath: /data/master
              name: data-master
      volumes:
        - name: data-master
          persistentVolumeClaim:
            claimName: seaweedfs-master-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: seaweedfs-master
  namespace: seaweedfs
spec:
  selector:
    app: seaweedfs-master
  ports:
    - name: http
      port: 9333
    - name: grpc
      port: 19333
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: seaweedfs-master-pv
  namespace: seaweedfs
spec:
  storageClassName: local-path
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /Users/ecmaxp/seaweedfs/master
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seaweedfs-master-pvc
  namespace: seaweedfs
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
