apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mattermost-pvc
  namespace: mattermost
spec:
  storageClassName: shared-path-protected
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
